% normally distributed random variables
z = randn(1,1000);
z
hist(z); %gives distribution of z as histograms
mean = 0;
for i=1:1000
mean = mean + z(i);
end
mean = mean/1000;
mean
y1 = mean(z);
y1
variance = 0;
for i=1:1000
variance = variance + ((z(i)-mean)*(z(i)-mean));
end
variance = variance/1000;
variance
% normal gaussain rv
x = -20:0.1:20;
mean = 3;
var = 4;
pdfnormal = normpdf(x,mean,var);
subplot(1,3,1);
plot(x,pdfnormal),axis([-20 20 0 0.1]);
